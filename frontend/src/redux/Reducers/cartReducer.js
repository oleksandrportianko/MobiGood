let initialState = {
  countItemsCart: 0,
};

let cartReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default cartReducer;
