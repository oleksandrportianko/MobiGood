let initialState = {
  countItemsCart: 10,
};

let cartReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default cartReducer;
